import { ValidationError } from "class-validator";
export interface ValidationErrorInfo {
    message: string;
    details: string[];
}
export declare function getValidationErrorInfo(validationErrors: ValidationError[]): ValidationErrorInfo;
