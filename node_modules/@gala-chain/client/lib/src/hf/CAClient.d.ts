import { Wallet } from "fabric-network";
import { Identity } from "fabric-network/lib/impl/wallet/identity";
export declare class CAClient {
    readonly orgMsp: string;
    readonly caHostName: string;
    readonly asLocalhost: boolean;
    readonly affiliation: string;
    readonly wallet: Promise<Wallet>;
    readonly enrolledAdmin: Promise<Identity>;
    readonly adminId: string;
    private readonly caClient;
    constructor(orgMsp: string, adminId: string, adminSecret: string, connectionProfile: Record<string, unknown>);
    isReady(): Promise<boolean>;
    getIdentityOrRegisterUser(userId: string): Promise<Identity>;
}
