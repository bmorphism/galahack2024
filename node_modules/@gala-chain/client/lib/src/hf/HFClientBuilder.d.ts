import { ChainClientBuilder, ContractConfig } from "../generic";
import { HFClient } from "./HFClient";
export declare class HFClientBuilder extends ChainClientBuilder {
    readonly orgMsp: string;
    private readonly connectionProfile;
    readonly useServiceDiscovery: boolean;
    readonly asLocalhost: boolean;
    private readonly caClient;
    constructor(orgMsp: string, adminId: string, adminSecret: string, connectionProfile: Record<string, unknown>);
    forContract(cfg: ContractConfig): HFClient;
    private buildConnectedGateway;
}
