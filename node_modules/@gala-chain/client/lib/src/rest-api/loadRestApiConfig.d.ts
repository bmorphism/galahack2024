import { Schema } from "jsonschema";
export interface ContractConfig {
    pathFragment: string;
    chaincodeName: string;
    contractName: string;
}
export interface ChannelConfig {
    pathFragment: string;
    channelName: string;
    contracts: ContractConfig[];
}
export interface RestApiConfig {
    channels: ChannelConfig[];
}
export declare const restApiJsonSchema: Schema;
export declare function loadRestApiConfig(configPath: string): RestApiConfig;
