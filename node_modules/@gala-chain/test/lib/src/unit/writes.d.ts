import { ChainObject, RangedChainObject } from "@gala-chain/api";
export type WriteRecord = ChainObject | RangedChainObject | {
    key: string;
    isDelete: true;
} | {
    key: string;
    value: string;
};
export declare function writesMap(...records: WriteRecord[]): Record<string, string>;
