import { ChainError } from "@gala-chain/api";
export declare function transactionSuccess<T>(payload?: T): unknown;
export declare function transactionError(matcher?: string | unknown | ChainError): unknown;
export declare function transactionErrorKey(key: string): {
    asymmetricMatch(other: unknown): boolean;
    toString(): string;
    getExpectedType?(): string;
    toAsymmetricMatcher?(): string;
};
export declare function transactionErrorCode(code: number): {
    asymmetricMatch(other: unknown): boolean;
    toString(): string;
    getExpectedType?(): string;
    toAsymmetricMatcher?(): string;
};
export declare function transactionErrorMessageContains(s: string): {
    asymmetricMatch(other: unknown): boolean;
    toString(): string;
    getExpectedType?(): string;
    toAsymmetricMatcher?(): string;
};
