import { TokenBalance, TokenInstanceKey } from "@gala-chain/api";
import BigNumber from "bignumber.js";
import { GalaChainContext } from "../types";
export interface UnlockTokenParams {
    tokenInstanceKey: TokenInstanceKey;
    name: string | undefined;
    quantity: BigNumber | undefined;
}
export declare function unlockToken(ctx: GalaChainContext, { tokenInstanceKey, name, quantity }: UnlockTokenParams): Promise<TokenBalance>;
export declare function unlockFungibleToken(ctx: GalaChainContext, { tokenInstanceKey, name, quantity }: UnlockTokenParams): Promise<TokenBalance>;
export declare function unlockTokens(ctx: GalaChainContext, params: UnlockTokenParams[]): Promise<TokenBalance[]>;
