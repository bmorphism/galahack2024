import { GalaChainContext } from "../types";
export declare class UniqueTransactionService {
    private static UT_INDEX_KEY;
    private static getUniqueTransactionKey;
    private static putUniqueTransaction;
    private static ensureUniqueKey;
    static ensureUniqueTransaction(ctx: GalaChainContext, uniqueKey: string): Promise<void>;
}
