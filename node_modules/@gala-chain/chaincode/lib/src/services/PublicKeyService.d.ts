import { ChainCallDTO, PublicKey, UserProfile, normalizePublicKey } from "@gala-chain/api";
import { Context } from "fabric-contract-api";
import { GalaChainContext } from "../types";
export declare class PublicKeyService {
    private static PK_INDEX_KEY;
    private static UP_INDEX_KEY;
    private static getPublicKeyKey;
    private static getUserProfileKey;
    static normalizePublicKey: typeof normalizePublicKey;
    static putPublicKey(ctx: GalaChainContext, publicKey: string, userId?: string): Promise<void>;
    static putUserProfile(ctx: GalaChainContext, ethAddress: string, userAlias: string): Promise<void>;
    static deleteUserProfile(ctx: GalaChainContext, ethAddress: string): Promise<void>;
    static getUserProfile(ctx: Context, ethAddress: string): Promise<UserProfile | undefined>;
    static getPublicKey(ctx: Context, userId: string): Promise<PublicKey | undefined>;
    /**
     * Verifies if the data is properly signed. Throws exception instead.
     */
    static ensurePublicKeySignatureIsValid(ctx: GalaChainContext, userId: string, dto: ChainCallDTO): Promise<PublicKey>;
}
