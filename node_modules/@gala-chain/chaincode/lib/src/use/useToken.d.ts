import { AuthorizedOnBehalf, TokenBalance, TokenInstanceKey } from "@gala-chain/api";
import { BigNumber } from "bignumber.js";
import { GalaChainContext } from "../types";
export interface UseTokenParams {
    owner: string;
    inUseBy: string;
    tokenInstanceKey: TokenInstanceKey;
    quantity: BigNumber;
    allowancesToUse: Array<string>;
    authorizedOnBehalf: AuthorizedOnBehalf | undefined;
}
export declare function useToken(ctx: GalaChainContext, { owner, inUseBy, tokenInstanceKey, quantity, allowancesToUse, authorizedOnBehalf }: UseTokenParams): Promise<TokenBalance>;
