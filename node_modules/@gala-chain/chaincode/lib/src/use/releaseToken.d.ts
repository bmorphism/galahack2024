import { TokenBalance, TokenInstanceKey } from "@gala-chain/api";
import { GalaChainContext } from "../types";
export interface ReleaseTokenParams {
    tokenInstanceKey: TokenInstanceKey;
}
export declare function releaseToken(ctx: GalaChainContext, { tokenInstanceKey }: ReleaseTokenParams): Promise<TokenBalance>;
