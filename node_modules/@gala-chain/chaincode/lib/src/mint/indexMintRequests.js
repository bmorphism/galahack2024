"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.indexMintRequests = void 0;
function indexMintRequests(requests) {
    var _a;
    const hash = {};
    for (let i = 0; i < requests.length; i++) {
        const { collection, category, type, additionalKey } = requests[i];
        const key = `${collection}_${category}_${type}_${additionalKey}`;
        hash[key] = (_a = hash[key]) !== null && _a !== void 0 ? _a : [];
        hash[key].push(requests[i]);
    }
    return hash;
}
exports.indexMintRequests = indexMintRequests;
