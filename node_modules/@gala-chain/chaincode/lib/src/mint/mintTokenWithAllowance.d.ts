import { TokenClassKey, TokenInstanceKey } from "@gala-chain/api";
import { BigNumber } from "bignumber.js";
import { GalaChainContext } from "../types/GalaChainContext";
export interface MintTokenWithAllowanceParams {
    tokenClassKey: TokenClassKey;
    tokenInstance: BigNumber;
    owner: string;
    quantity: BigNumber;
}
export declare function mintTokenWithAllowance(ctx: GalaChainContext, dto: MintTokenWithAllowanceParams): Promise<Array<TokenInstanceKey>>;
