import { TokenBalance, TokenClass, TokenInstance, TokenMintFulfillment } from "@gala-chain/api";
import BigNumber from "bignumber.js";
import { GalaChainContext } from "../types";
export declare function constructVerifiedMints(ctx: GalaChainContext, dto: TokenMintFulfillment, tokenClass: TokenClass, instanceCounter: BigNumber): Promise<[TokenInstance[], TokenBalance]>;
