import { TokenBalance } from "@gala-chain/api";
import { GalaChainContext } from "../types";
export interface FetchBalancesParams {
    collection?: string;
    category?: string;
    type?: string;
    additionalKey?: string;
    owner: string;
}
export declare function fetchBalances(ctx: GalaChainContext, data: FetchBalancesParams): Promise<TokenBalance[]>;
