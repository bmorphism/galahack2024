import { ChainCallDTO } from "@gala-chain/api";
import { GalaChainContext } from "../types";
/**
 *
 * @param ctx
 * @param dto
 * @param legacyCAUser fallback user alias to use then the new flow is not applicable
 * @returns User alias of the calling user.
 */
export declare function authorize(ctx: GalaChainContext, dto: ChainCallDTO | undefined, legacyCAUser: string): Promise<{
    alias: string;
    ethAddress?: string;
}>;
export declare function ensureIsAuthorizedBy(ctx: GalaChainContext, dto: ChainCallDTO, userAlias: string): Promise<{
    alias: string;
    ethAddress?: string;
}>;
export declare function ensureOrganizationIsAllowed(ctx: GalaChainContext, allowedOrgsMSPs: string[] | undefined): void;
