import { BurnTokenQuantity, TokenBurn } from "@gala-chain/api";
import { GalaChainContext } from "../types";
export interface BurnsTokensParams {
    owner: string;
    toBurn: BurnTokenQuantity[];
    preValidated?: boolean;
}
export declare function burnTokens(ctx: GalaChainContext, { owner, toBurn, preValidated }: BurnsTokensParams): Promise<TokenBurn[]>;
