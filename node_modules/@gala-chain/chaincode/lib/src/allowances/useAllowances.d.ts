import { TokenAllowance } from "@gala-chain/api";
import { BigNumber } from "bignumber.js";
import { GalaChainContext } from "../types";
export declare function useAllowances(ctx: GalaChainContext, quantity: BigNumber, applicableAllowances: TokenAllowance[]): Promise<boolean>;
