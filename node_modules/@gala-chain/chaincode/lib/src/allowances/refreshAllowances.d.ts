import { AllowanceKey, TokenAllowance } from "@gala-chain/api";
import BigNumber from "bignumber.js";
import { GalaChainContext } from "../types";
export interface AllowanceType {
    allowanceKey: AllowanceKey;
    uses: BigNumber;
    expires: number;
}
export declare function refreshAllowances(ctx: GalaChainContext, allowances: AllowanceType[]): Promise<TokenAllowance[]>;
