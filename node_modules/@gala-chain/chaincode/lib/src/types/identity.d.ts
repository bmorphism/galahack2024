import { Identity as BaseIdentity } from "fabric-network";
export interface IIdentity extends BaseIdentity {
    credentials: {
        certificate: string;
        privateKey: string;
    };
    version: number;
}
